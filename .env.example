# =============================================================================
# CONFIGURACIÓN DE ENTORNO - GESTOR HR V3.0 PROFESIONAL
# =============================================================================
# Copia este archivo como .env y modifica los valores según tu configuración

# Información del proyecto
PROJECT_NAME="Gestor HR v3.0 Profesional"
VERSION="1.0.0"
DESCRIPTION="Sistema profesional de gestión hotelera"

# Configuración del servidor
HOST=0.0.0.0
PORT=8000
DEBUG=true

# Base de datos PostgreSQL
DATABASE_URL=postgresql://postgres:password@localhost:5432/gestor_hr_v3
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10

# Seguridad (CAMBIAR EN PRODUCCIÓN)
SECRET_KEY=your-super-secret-key-change-this-in-production-make-it-very-long
ACCESS_TOKEN_EXPIRE_MINUTES=30
ALGORITHM=HS256

# CORS - Orígenes permitidos (separados por comas)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000

# Configuración de logs
LOG_LEVEL=DEBUG
LOG_FILE=logs/app.log

# Email (opcional - para notificaciones futuras)
SMTP_TLS=true
SMTP_PORT=587
SMTP_HOST=smtp.gmail.com
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# =============================================================================
# INSTRUCCIONES:
# 1. Copia este archivo como .env en la raíz del proyecto
# 2. Modifica los valores según tu configuración local
# 3. NUNCA commitees el archivo .env al repositorio
# 4. En producción, usa variables de entorno del sistema
# =============================================================================
